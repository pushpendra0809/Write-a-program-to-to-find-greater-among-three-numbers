#include <iostream>
using namespace std;
int main()
{
    int a, b,c ;
    cin>>a>>b>>c;
    if(a>=b && a>=c)
    cout<<"yes"<<a;
    else
    if(b>=a && b>=c)
    cout<<"yes"<<b;
    else 
    if(c>=a && c>=b)
    cout<<"yes"<<c;
} 
